## Agent instructions (mandatory)

Before starting any task, every agent must:

1. Read `docs/ASSIGNMENT_REQUIREMENTS.md`.
2. Read `docs/ruoli.md`.
3. Follow both files strictly while working.
4. Identify and declare the active role before writing anything:
   - `matilde`
   - `stefan`
   - `marco`

No task should be executed without first checking requirements and role guardrails.
